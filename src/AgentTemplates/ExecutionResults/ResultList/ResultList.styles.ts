import {css} from "@emotion/css";

export const useStyles = () => {
    return {
        treeView: css`
            overflow-y: auto;
            display: flex;
            padding-right: 12px;
        `
    }
}
