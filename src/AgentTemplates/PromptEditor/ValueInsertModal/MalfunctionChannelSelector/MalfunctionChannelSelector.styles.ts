import { css } from "@emotion/css"

export const useStyles = () => {
    return {
        malfunctionChannelSelector: css`
            display: flex;
            flex-direction: column;
            gap: 12px;
        `
    }
}
